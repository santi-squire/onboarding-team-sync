# Handoff — onboarding-team-sync · 2026-05-27 (weekly day)

Paste this into a fresh Claude Code terminal to continue. Self-contained.

## What this is
Working dir: `/Users/santianaya/code/onboarding-team-sync` (git, push to main allowed).
Onboarding team's login A/B experiment tracker + weekly decks + ongoing verification/courses work for Squire. User is Santiago Anaya (reply in rioplatense Spanish, casual, no em-dashes; code/commits in English).

**Read `docs/SESSION_HANDOFF.md` first** — full context (last full rewrite May 22). This file adds the May 27 state on top.

## Maestri canvas
- Connected agent: **Tracker** (a plain shell, NOT an AI — commands you `maestri ask "Tracker" "..."` run literally as shell. It runs `python3 -m http.server 8767` serving the repo).
- Portals: **Live** (github pages), **Fresh** (localhost:8767, was showing deck 20), **Deck 27** (localhost:8767/weekly/2026-05-27/).
- `maestri list` to see them; `maestri portal navigate "Deck 27" "url"` to refresh.

## Data state · May 27 refresh (in data/2026-05-08.json)
Sample 2,662 NEW devices. The experiment story has CONVERGED — be honest:
- Completion gap +2.9pp B (trend +14 → +8.7 → +6.7 → +3.6 → +2.9). Login success rate now TIED/slightly A (50.1 A / 49.5 B).
- B's durable wins: welcome reach +7pp (email-first auto-loads), forgot-password −19pp (45% A / 26% B), catches ~22% invited barbers before duplicate.
- Accidental indies proxy eroded −75% → −36% as weeks reclassify up. CS ground-truth is the clean signal: 3 accidental cancellations April, 0 in May (population-wide, NOT B-specific — it's new-flow-vs-old).
- Login abandons B higher +5pp (mechanical, 2 surfaces). 81% of abandoners return.
- Scope leak flat at 3 devices (PR #4143 shipped in 3.23.1; leftover 3.23.0 devices).

## Refreshing analytics
`snow sql --connection default --format json -q "..."` (externalbrowser SSO, first run opens browser; queries often hang on SSO — just re-run). The 10 standard queries (Q1-Q10) are in `docs/SESSION_HANDOFF.md`. After pulling, update `data/2026-05-08.json` (rolloutContext, postRolloutNew, scorecard, historicalComparison weeks) + the hardcoded bar chart in `tracker/index.html` (search "BAR CHART"). Validate: `node -e "JSON.parse(require('fs').readFileSync('data/2026-05-08.json'));console.log('OK')"`.

## Deck weekly/2026-05-27/index.html (built, NOT pushed yet)
Same structure as weekly/2026-05-20. Sections:
1. The call — recommend Flow B, Descope screenshots, 3 pillars (−19% forgot-pwd / +7pp welcome / 3→0 CS) + honesty note that completion converged.
2. All-hands slide — bar chart (Jan 5 → May 27, post bars at 0 = CS ground truth, NO asterisks per user), "what this unlocked" (3→0 CS, −45% login failures, −40% duplicates). Eric's quote was REMOVED.
3. Courses scope — no video (user changed design). "Out of V1 scope" grid (CSV/wipe/reviews/Flex-Operator-SMS) + "could make V1" (Course 1 RN done, Course 3, credential imports) + pending analytics decision (Tristan).
4. Verification — 2 screenshots (login + SMS verify). Shipped-stopgap vs proposed-defer cards. FigJam embedded (board wwEiY53MIasNSEA4AfPkx2 node 686-4839). Framing: middleware (short) vs defer barber creation (long). RN-only open question.
5. Decisions needed today — 3 cards w/ owners: indies-only vs all (Eric), email verification when/how (Yurii/Tristan), defer timeline (Guido/Yurii).
6. Tracker link.

## Verification facts (corrected — earlier draft was wrong)
- Schema flags phoneVerified/emailVerified exist since Dec 2024 BUT barber→user sync was broken until May 2026 (PR #18192 + backfill).
- Shipped = login GATE stopgap (PRs #18180/#18185/#18192, v0.36.2, cutoff flag default 2026-05-19). Creates barber then blocks login. Generic "wrong credentials", no structured error.
- Defer barber creation (Yurii/Guido) = proposed, NO ticket yet. The real structural fix.
- Gate is fragile: runs before MFA branch by execution order, not design. New auth endpoints born ungated. Middleware = immediate coverage; defer = eliminates the problem.
- Descope owns Staff Management only today; full signup ownership in progress (SQR-12413).
- Tristan's adds: all users verify on login (lean phone) + "remember device" (note: board chose cross-device BE flag, not per-device storage); MFA device mgmt = follow-up out of scope.

## Forgot-password misclick analysis (done today, strong finding)
Flow A: 36% of forgot-password tappers log in within 5 min (knew password = misclick), 57% eventually log in, 2.4 avg taps (max 28). Flow B: 10% / 20% / 1.6 taps (max 8). → Email-first kills the "tapped forgot-password thinking it's sign-in" confusion. Draft sent to Tristan. NOT yet added to deck — user may want it in the forgot-password pillar.

## Open / next
- Push deck 27 to prod when user says go (not pushed).
- FigJam: embedded live iframe; offer PNG fallback if it doesn't load on screen-share.
- Course 1 RN: mentioned in "could make V1"; user deciding whether to add a demo.
- CS re-pull through May 27 pending (ask McChesney via Tristan).
- Beauty PLG discovery brief is a separate workstream (see SESSION_HANDOFF.md).
