# Handoff — onboarding-team-sync · 2026-06-02 (Tuesday, day before June 3 weekly)

Paste this into a fresh Claude Code terminal to continue. Self-contained.

## What this is
Working dir: `/Users/santianaya/code/onboarding-team-sync` (git, push to main allowed).
Onboarding team's login A/B experiment tracker + weekly decks + verification/courses work for Squire. User is Santiago Anaya (reply in rioplatense Spanish, casual, no em-dashes; code/commits in English).

**Read order:** `docs/SESSION_HANDOFF.md` (full context, May 22 rewrite) → `docs/HANDOFF_2026-05-27.md` (May 27 state) → this file (June 2 state on top).

## What this session did (June 2)

### Deck 27 — misclick block added (DONE, committed, pushed)
- Closed the open item from the May 27 handoff ("NOT yet added to deck"): the forgot-password misclick analysis is now in the "The call" section of `weekly/2026-05-27/index.html` (~lines 495-525).
- Table A vs B: misclick rate (logged in within 5 min) 36% / 10%, eventual login 57% / 20%, avg taps 2.4 / 1.6.
- Includes the "next on forgot-password" note: testing an always-tappable login button.
- Already in `66f66eb`; main == origin/main at `621dce1`. Nothing pending in the tree.

### Repo state
- HEAD = `621dce1` "Refresh tracker to May 29 · 3,016 NEW · flag SRM drift (sliding-window artifact)". Clean, synced with origin.
- A second agent ("Main", another Claude session) announced it would commit+push a tracker refresh (Snowflake pull → `data/2026-05-08.json` + `tracker/index.html`). **As of this handoff that commit has NOT landed.** Before touching those two files, `git pull` and check for Main's commit.
- This session only touched `weekly/2026-05-27/index.html`; data/tracker files were deliberately left to Main.

### Local server (relaunched, verified up)
- The Maestri "Tracker" shell agent (which ran `python3 -m http.server 8767`) had died — port free, shell back at prompt.
- Relaunched the server in background from this terminal. Verified: HTTP 200 on `http://localhost:8767/weekly/2026-05-27/index.html`.
- If it dies again: `python3 -m http.server 8767` from repo root.

### Maestri portals — flaky, left dirty
- Portal tooling broke mid-session: portals show in `maestri list` but info/screenshot/navigate/evaluate return "portal not found", and navigate truncates URLs to the bare host (`localhost:8767` root → repo listing, not the deck).
- **Two duplicate portals left on the canvas: "Deck 27" and "Deck27"**, both pointing at the localhost:8767 root. Cleanup pending — `portal close` is destructive, user has NOT approved it yet.
- Workarounds discussed: (a) user opens the deck URL directly in browser, (b) Main manages portals (its "Live Tracker" portal was stable).

## Open / next
1. **Portal cleanup** — close "Deck 27" + "Deck27" duplicates once the user OKs it (destructive).
2. **Watch for Main's tracker-refresh commit** — pull before any edit to `data/2026-05-08.json` or `tracker/index.html`.
3. **June 3 weekly (tomorrow, Wednesday)** — deck 27 now has the misclick block; decide if it's reused as-is or a `weekly/2026-06-03/` is made after Main's data refresh lands.
4. Carry-overs from May 27 handoff, not addressed this session: CS re-pull through May 27 (McChesney via Tristan), Course 1 RN demo decision, FigJam PNG fallback for screen-share.
